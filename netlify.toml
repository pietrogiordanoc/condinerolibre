[build]
  publish = "."
  command = "echo 'No build command needed - static site'"

# Permitir iframes del mismo origen globalmente
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "SAMEORIGIN"

# Comentado: Este redirect interfiere con los assets del radar
# [[redirects]]
#   from = "/*"
#   to = "/index.html"
#   status = 200
#   force = false
